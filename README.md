Rough summary of various iterations in absence of github commits

The first version of the code didnt link to mongoDb but rather used a CSV file with mock data. 
When I was happy with the AI server then the live database was added.
There were fewer personas originally and this got edited over time.
Originally there were only a few suggestions per persona and this meant that the AI recommendation was repetitive. So more suggestions were added.
Last the API was included in to the code so that the Front End can reach this server. 
